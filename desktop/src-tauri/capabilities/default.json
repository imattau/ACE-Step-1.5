{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal IPC access for the local startup screen",
  "windows": ["main"],
  "permissions": ["core:default"]
}
